# ============================================================
# FastAPI AI Scheduler - Environment Configuration
# ============================================================

# Core Application Info
APP_NAME=AI Scheduler
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Runtime Flags
# Set to 1 to bypass the database and use fake in-memory storage
USE_FAKE_DB=1
USE_LOCAL_DB=0

# Database Config (used when USE_FAKE_DB=0)
DB_URL=sqlite:///./scheduler.db

# Security
JWT_SECRET=CHANGE_ME_BEFORE_DEPLOY
